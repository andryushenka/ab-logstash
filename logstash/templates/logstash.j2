input {
udp {
host => "logstash_private_ip"
port => {{ webui_port }}
codec => "json"
type => "rsyslog"
}
}

output {
if [type] == "rsyslog" {
elasticsearch {
hosts => [ "{{ inventory_hostname }}:9200" ] }
}
}